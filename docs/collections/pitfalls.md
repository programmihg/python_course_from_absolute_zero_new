# Pitfalls
2. Попълване на docs/collections/pitfalls.md
Това е изключително важна тема, която обяснява как работи паметта при копиране на колекции.

Markdown

# Чести грешки при колекциите

Най-голямата клопка в Python е свързана с това как списъците се съхраняват в паметта.

### Проблемът с препратките (References)
Когато напишете `list_b = list_a`, вие **не създавате нов списък**, а просто нов етикет към същия обект в паметта.

```python
a = [1, 2, 3]
b = a
b.append(4)
print(a)  # Изход: [1, 2, 3, 4] -> Списъкът 'a' също се е променил!
```

Как да копираме правилно?
За да създадете независимо копие, използвайте метода .copy() или slicing:

```python
true_copy = a.copy()
# или
true_copy = a[:]
```

🧠 Упражнение: Памет и Копиране
<div class="flashcard-container"> <div class="flashcard"> <div class="flashcard-front">Какво се случва при операцията list_b = list_a?</div> <div class="flashcard-back">Двете променливи започват да сочат към един и същ обект в паметта.</div> </div> </div>