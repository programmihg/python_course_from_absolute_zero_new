# Indexing
-----

### 2\. Попълване на `docs/strings/indexing.md`

Индексирането и "нарязването" (slicing) са сред най-мощните инструменти в Python.

````markdown
# Индексиране и Slicing

Можем да достъпваме части от низа чрез квадратни скоби `[]`.

### Индекси:
* Първи символ: `s[0]`
* Последен символ: `s[-1]` (отрицателно индексиране)

### Slicing (Нарязване):
Синтаксисът е `[start:stop:step]`.
* `s[0:5]` — взима символите от индекс 0 до 4 (индекс 5 е изключен).
* `s[::-1]` — обръща целия низ наобратно.

```python
language = "Python"
print(language[0:2]) # Py
print(language[-1])  # n
```

🧠 Упражнение: Индексиране
<div class="flashcard-container"> <div class="flashcard"> <div class="flashcard-front">Как ще вземете последните 3 символа от низ "s"?</div> <div class="flashcard-back">Чрез s[-3:]</div> </div> </div>

<div class="flashcard-container"> <div class="flashcard"> <div class="flashcard-front">Какъв е резултатът от "Hello"[1]?</div> <div class="flashcard-back">'e' (индексацията започва от 0).</div> </div> </div>