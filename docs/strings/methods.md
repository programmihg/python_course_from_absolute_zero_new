# Methods
---------------
# Попълване на docs/strings/methods.md
Тук описваме най-популярните методи за работа с текст.

# Методи на низовете

Низовете в Python имат вградени методи, които ни позволяват да ги манипулираме лесно. Важно е да запомните, че тези методи **връщат нов низ**, а не променят оригиналния.

### Често използвани методи:
* `.upper()` / `.lower()` — Променя регистъра на буквите.
* `.strip()` — Премахва излишните интервали в началото и края.
* `.replace("старо", "ново")` — Заменя част от текста с друг.
* `.split(",")` — Разделя низа на списък по даден символ.

---

## 🧠 Упражнение: Методи
<div class="flashcard-container">
<div class="flashcard">
<div class="flashcard-front">Какво ще върне "  Python  ".strip()?</div>
<div class="flashcard-back">"Python" (без интервалите отстрани).</div>
</div>
</div>