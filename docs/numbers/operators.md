# Operators
### 2\. Попълване на `docs/numbers/operators.md`

```markdown
# Аритметични оператори

Освен стандартните оператори, Python предлага специфични начини за деление.

### Видове деление:
* `/` (True Division): Винаги връща **float** (напр. `10 / 2 = 5.0`).
* `//` (Floor Division): Връща само цялата част, закръглена надолу (напр. `10 // 3 = 3`).
* `%` (Modulo): Връща остатъка от делението (напр. `10 % 3 = 1`).

### Други оператори:
* `**` : Степенуване (напр. `2 ** 3 = 8`).
* `abs()` : Връща абсолютната стойност.

---

## 🧠 Упражнение: Оператори
<div class="flashcard-container">
<div class="flashcard">
<div class="flashcard-front">Какъв е резултатът от 7 // 2?</div>
<div class="flashcard-back">3 (целочислено деление).</div>
</div>
</div>

<div class="flashcard-container">
<div class="flashcard">
<div class="flashcard-front">Какъв е типът на резултата от 4 / 2?</div>
<div class="flashcard-back">float (резултатът е 2.0).</div>
</div>
</div>