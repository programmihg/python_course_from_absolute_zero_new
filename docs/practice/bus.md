# Bus
-----

### 2\. Попълване на `docs/practice/bus.md`

Тук упражняваме цикъла `while` и капацитета на колекциите.

````markdown
# Проект: Управление на Автобус

Ще симулираме качването на пътници в автобус, докато се запълни капацитетът му.

### Логика:
Използваме `while` цикъл, който следи броя на свободните места.

```python
capacity = 10
passengers = 0

while passengers < capacity:
    new_group = int(input("Колко души искат да се качат? "))
    if passengers + new_group <= capacity:
        passengers += new_group
        print(f"Качени пътници. Свободни места: {capacity - passengers}")
    else:
        print(f"Няма място за всички! Свободни места: {capacity - passengers}")

print("Автобусът е пълен. Тръгваме!")
```

🧠 Упражнение: Автобус
<div class="flashcard-container"> <div class="flashcard"> <div class="flashcard-front">Какво ще се случи, ако зададем условие "while True" в тази задача?</div> <div class="flashcard-back">Ще ни трябва команда "break", за да спрем цикъла, когато капацитетът се запълни.</div> </div> </div>