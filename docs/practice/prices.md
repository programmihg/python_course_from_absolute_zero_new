# Prices
-----

### 5\. Попълване на `docs/practice/prices.md`

Този проект симулира обработка на списък с данни и изчисляване на общи стойности.

````markdown
# Проект: Калкулатор на цени

Ще създадем програма, която обработва списък с цени, изчислява общата сума и средната цена.


### Логика:
Използваме вградените функции `sum()` и `len()`.

```python
prices = [12.50, 4.20, 18.00, 7.30, 25.00]

total = sum(prices)
average = total / len(prices)

print(f"Обща сума на покупките: {total:.2f} лв.")
print(f"Средна цена на продукт: {average:.2f} лв.")
```

🧠 Упражнение: Цени
<div class="flashcard-container"> <div class="flashcard"> <div class="flashcard-front">Коя функция връща сбора на всички числа в един списък?</div> <div class="flashcard-back">Вградената функция sum().</div> </div> </div>