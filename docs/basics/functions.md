# Functions
# Тук описваме инструментите за „дисекция“ на обекти, които ще ползвате често в терминала на PyCharm.
# Вградени функции за инспекция

За да разберем един обект, Python ни дава три мощни функции:

* `type(obj)` — Показва от кой клас е обекта.
* `id(obj)` — Показва уникалния му адрес в паметта.
* `dir(obj)` — Списък на всички атрибути и методи, налични за обекта.

---

## 🧠 Упражнение: Инспекция
<div class="flashcard-container">
<div class="flashcard">
<div class="flashcard-front">Коя функция ще ви покаже всички методи на даден обект?</div>
<div class="flashcard-back">Функцията dir().</div>
</div>
</div>