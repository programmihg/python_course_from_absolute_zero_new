# Variables
# В Python променливите не са „кутии“ за данни, а „етикети“ (references) към обекти.

# Променливи и препратки

В Python, когато напишем `x = 5`, ние не създаваме кутия `x`, в която слагаме `5`. Ние създаваме обект `5` в паметта и му закачаме етикет (препратка) с име `x`.

### Проверка на адреса с id()
Всеки обект има уникален адрес. Можем да го видим чрез функцията `id()`:
```python
a = [1, 2, 3]
b = a
print(id(a) == id(b))  # True - сочат към едно и също място
```

# 🧠 Упражнение: Променливи
<div class="flashcard-container"> <div class="flashcard"> <div class="flashcard-front">Какво всъщност е променливата в Python?</div> <div class="flashcard-back">Тя е препратка (reference) или етикет към обект в паметта.</div> </div> </div>

<div class="flashcard-container"> <div class="flashcard"> <div class="flashcard-front">Какво връща функцията id()?</div> <div class="flashcard-back">Уникалния идентификатор (адреса) на обекта в паметта.</div> </div> </div>