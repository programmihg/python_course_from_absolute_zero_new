# Terminal
# Тук се фокусираме върху базовите команди, които вече използваш за управление на проекта.
# Работа с терминала

Терминалът е директният интерфейс към операционната система. За един програмист е важно да може да се движи бързо между папките и да стартира скриптове без графичен интерфейс.

### Ключови концепции:
* **Shell**: Програмата, която интерпретира командите (bash, zsh, powershell).
* **Working Directory**: Папката, в която се намирате в момента (твоят проект е `python_from_absolute_zero`).

### Основни команди:
| Команда | Описание |
| --- | --- |
| `pwd` | Показва пълния път до текущата папка. |
| `ls` (mac/Linux) / `dir` (Windows) | Списък с файловете в директорията. |
| `cd <име>` | Влизане в папка (Change Directory). |
| `cd ..` | Връщане едно ниво нагоре. |

---

## 🧠 Упражнение: Терминал
<div class="flashcard-container">
<div class="flashcard">
<div class="flashcard-front">Как да се върна в по-горната папка чрез терминала?</div>
<div class="flashcard-back">Чрез командата: cd ..</div>
</div>
</div>