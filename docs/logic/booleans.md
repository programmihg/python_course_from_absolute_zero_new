# Booleans
# Преминаваме към раздел „Логика“. Това е критичен момент в обучението, защото тук кодът започва да „взема решения“ въз основа на данни. Според твоя nav план, този раздел включва три файла: booleans.md, logical_ops.md и conditions.md.
1. Попълване на docs/logic/booleans.md
Булевият тип (bool) има само две възможни стойности: True или False.

Markdown

# Булеви стойности (Booleans)

Булевият тип данни представя логическата истинност на едно твърдение. В Python те винаги започват с главна буква: `True` и `False`.

### Сравнителни оператори:
Тези оператори винаги връщат булева стойност:
* `==` — Равно на (внимавайте: не бъркайте с `=` за присвояване).
* `!=` — Различно от.
* `>` / `<` — По-голямо / По-малко.
* `>=` / `<=` — По-голямо или равно / По-малко или равно.

```python
x = 10
y = 5
print(x > y)   # True
print(x == 5)  # False
``

🧠 Упражнение: Булеви стойности
<div class="flashcard-container"> <div class="flashcard"> <div class="flashcard-front">Какъв е резултатът от израза 10 != 10?</div> <div class="flashcard-back">False (защото 10 е равно на 10).</div> </div> </div>