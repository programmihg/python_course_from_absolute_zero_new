# Logical Ops
-----

### 2\. Попълване на `docs/logic/logical_ops.md`

Логическите оператори ни позволяват да комбинираме няколко проверки в една.

[Image of truth tables for AND, OR, and NOT operators]

````markdown
# Логически оператори

Python използва думи вместо символи за своите логически оператори, което прави кода много четим.

### Основни оператори:
1. **and**: Връща `True` само ако **и двете** твърдения са верни.
2. **or**: Връща `True` ако **поне едно** от твърденията е вярно.
3. **not**: Обръща стойността (прави `True` на `False` и обратно).

```python
age = 20
has_license = True
```

# Проверка дали може да шофира
```python
can_drive = age >= 18 and has_license # True
```

🧠 Упражнение: Логика
<div class="flashcard-container"> <div class="flashcard"> <div class="flashcard-front">Какво връща изразът (True or False)?</div> <div class="flashcard-back">True (при 'or' е достатъчно само едното да е вярно).</div> </div> </div>
