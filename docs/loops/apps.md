# Apps
-----

### 3\. Попълване на `docs/loops/apps.md`

Тук ще разгледаме как циклите работят заедно с колекциите (списъците), които попълнихме по-рано.

````markdown
# Приложения на циклите

Циклите са най-полезни, когато трябва да обходим елементите на една колекция.

### Обхождане на списък с while:
```python
fruits = ["ябълка", "банан", "портокал"]
index = 0

while index < len(fruits):
    print(f"Аз обичам {fruits[index]}")
    index += 1
```
    
Валидация на вход:
Циклите са стандартен начин да подканяме потребителя, докато не получим валидни данни:

```python
age = ""
while not age.isdigit():
    age = input("Моля, въведете годините си с цифри: ")
```

🧠 Упражнение: Приложения
<div class="flashcard-container"> <div class="flashcard"> <div class="flashcard-front">Как можем да разберем дължината на списък в условието на цикъл?</div> <div class="flashcard-back">Чрез вградената функция len().</div> </div> </div>
