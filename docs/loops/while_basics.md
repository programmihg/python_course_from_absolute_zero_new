# While Basics
Преминаваме към раздел „Цикли“ (Loops). Това е моментът, в който кодът ви става истински мощен, защото позволява автоматичното повтаряне на задачи. Според вашия nav план в mkdocs.yml, тук имаме три файла: while_basics.md, control.md и apps.md.

1. Попълване на docs/loops/while_basics.md
Цикълът while изпълнява блок от код, докато определено условие е истина.

Markdown

# Основи на While цикъл

Цикълът `while` се използва за повтаряне на код, докато дадено логическо условие е `True`.

### Синтаксис:
```python
counter = 0
while counter < 5:
    print(f"Броячът е: {counter}")
    counter += 1  # Важно: променяйте условието, за да не стане безкраен цикъл!
```
    
Кога да го използваме?
Използваме while, когато не знаем предварително точно колко пъти ще трябва да се повтори действието (например: докато потребителят не въведе "exit").

🧠 Упражнение: While
<div class="flashcard-container"> <div class="flashcard"> <div class="flashcard-front">Какво ще се случи, ако условието на while винаги е True?</div> <div class="flashcard-back">Ще се получи "безкраен цикъл", който може да доведе до забиване на програмата.</div> </div> </div>